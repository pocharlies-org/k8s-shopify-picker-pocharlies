apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - { name: PORT, value: "3480" }
          envFrom:
            - secretRef: { name: picker-secrets }
